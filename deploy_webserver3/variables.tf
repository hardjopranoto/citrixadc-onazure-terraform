# Defining the variables for admin_username and admin_password for the VM
# The actual value will be asked during runtime

variable "admin_username" {
  type = string
}

variable "admin_password" {
  type = string
}